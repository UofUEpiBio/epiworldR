% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bubbles.R
\name{bubbles}
\alias{bubbles}
\alias{sample_household_sizes}
\title{Social bubbles intervention for network models}
\usage{
bubbles(
  model,
  household_id,
  flavor = c("household", "peer"),
  group_size,
  transmission_factor = 1,
  start_day = 0L,
  end_day = -1L,
  rewire_every = 0L,
  name = "Social bubble",
  max_households = 2L
)

sample_household_sizes(n_agents, min_size = 2L, max_size = 6L)
}
\arguments{
\item{model}{An object of class \link{epiworld_model} whose agents and contact
network have already been created.}

\item{household_id}{Integer vector of household labels, one per agent, in
agent order; its length must equal the number of agents. Labels are arbitrary
and need not be consecutive. Agents sharing a label form a household and are
always placed in the same bubble.}

\item{flavor}{Character scalar. \code{"household"} if whole households choose a
bubble together, \code{"peer"} if individuals choose contacts (see Details).}

\item{group_size}{Integer scalar. For \code{"household"}, the maximum number of
households per bubble (\code{1} gives a strict household-only lockdown). For
\code{"peer"}, the maximum number of contacts each agent may keep outside its
household.}

\item{transmission_factor}{Numeric scalar in [0, 1]. Multiplier applied to
transmission \emph{within} a bubble: \code{1} leaves it untouched, \code{0.5} halves it
(physical distancing), \code{0} blocks it entirely. Transmission \emph{between} bubbles
is always blocked.}

\item{start_day}{Integer scalar. First day on which the policy applies.}

\item{end_day}{Integer scalar. Day on which the policy is lifted (exclusive);
a negative value means it never lifts. Must be greater than \code{start_day}.}

\item{rewire_every}{Integer scalar. Re-randomize the bubbles every this-many
days, for policies whose contacts change over time; \code{0} keeps them fixed.}

\item{name}{Character scalar. Name given to the intervention's tool and event.}

\item{max_households}{Integer scalar, \code{"peer"} flavor only. The largest number
of households a bubble may contain; a nomination that would exceed it is
declined. This is what stops one household's choices from chaining into the
next (see Details). The default, \code{2}, represents two households joined by a
close contact. Ignored when \code{flavor = "household"}, where \code{group_size} is
already the cap.}

\item{n_agents}{Integer scalar. The total number of agents to be divided
into households.}

\item{min_size, max_size}{Integer scalar. The minimum and maximum size of
each household.}
}
\value{
Invisibly returns \code{model}, which is modified in place.

The function \code{sample_household_sizes()} returns an integer vector of
household sizes that sum to \code{n_agents}, with each size between \code{min_size}
and \code{max_size}.
}
\description{
A "social bubble" policy lets people keep seeing a small, fixed set of others
while cutting off the rest of their contacts. \code{bubbles()} implements such a
policy for models built on an explicit contact network (e.g. \link{ModelSEIR} or
\link{ModelSIR} after \code{\link[=agents_smallworld]{agents_smallworld()}} or \code{\link[=agents_from_edgelist]{agents_from_edgelist()}}), following
the household-based rules used during COVID-19.
}
\details{
The contact network is \strong{not} modified. Instead, every agent receives a tool
that, on each exposure, compares the bubble of the susceptible agent with the
bubble of the infectious one: if they differ, transmission is blocked; if they
match, transmission is multiplied by \code{transmission_factor}. Because contact
weights are uniform in these models, this is equivalent to deleting
out-of-bubble contacts, while leaving the network available for other purposes
(such as contact tracing or network summaries).
\subsection{How bubbles are formed}{

Bubbles are always disjoint (each agent is in exactly one), never split a
household, and are only ever built between households that are \strong{actually
connected} in the contact network.

That last property matters: the intervention can only suppress transmission
along existing edges, never create new ones, so placing two households that
share no contact in the same bubble would change nothing. Pairing households
at random would make \code{group_size} inert -- indistinguishable from a strict
lockdown however large the bubbles are. It also matches the real policy: a
household picks a bubble partner it already socialises with.
}

\subsection{The algorithms}{

Both rules start from the \strong{household contact graph}: one node per
household, with an edge between two households whenever at least one member
of the first is connected to a member of the second in the agents' contact
network. Every random draw uses the model's generator, so a run is
reproducible from its seed.

\code{flavor = "household"} grows bubbles from seed households:
\enumerate{
\item Visit the households in random order.
\item Skip a household if it already belongs to a bubble. Otherwise open a new
bubble containing it, and take its unassigned neighbours in the household
contact graph as the \emph{frontier}.
\item While the bubble holds fewer than \code{group_size} households and the frontier
is not empty, draw a household from the frontier at random, add it, and
extend the frontier with that household's unassigned neighbours. A
household tied to several members of the bubble appears in the frontier
more than once and is more likely to be drawn, so stronger ties are
favoured.
\item Stop when no unassigned neighbour is left, even if the bubble is smaller
than \code{group_size}.
}

Each bubble is therefore a \emph{connected} group of households, though not
necessarily one where every pair is directly tied: with \code{group_size > 2}, two
households in the same bubble may be linked only through a third. Households
with no available partner stay on their own, so there are always at least
\code{ceiling(n_households / group_size)} bubbles.

\code{flavor = "peer"} has agents choose from the contacts still available:
\enumerate{
\item Visit the agents in random order.
\item Skip an agent whose household is already in a full bubble: it has left the
pool and can neither choose nor be chosen.
\item Otherwise draw one of the agent's contacts outside its own household, at
random and without replacement, repeating until the agent has made
\code{group_size} successful choices or no contact is left that its bubble can
still take in. A draw is accepted when the two households are in different
bubbles \emph{and} the merged bubble would hold at most \code{max_households}
households; otherwise that contact is unavailable and the agent draws
again. Accepting merges the two households (household commitment: choosing
a peer brings both households into the bubble), and the agent stops as soon
as its bubble is full.
}

The cap is essential. Without it the merges percolate: with a few members per
household each choosing someone, the household graph becomes connected and the
entire population ends up in a single bubble, imposing no restriction at all.

In practice \code{max_households} is the dial that sets bubble size, while
\code{group_size} rarely binds: since a choice by \emph{any} member commits the whole
household, one household's members tend to fill its bubble however many
choices each of them is allowed. With \code{max_households = 2} a single accepted
choice fills the bubble, so \code{group_size} has no effect at all. Households whose
every contact was taken first stay on their own.
}

\subsection{Timing and replicates}{

The bubbles are drawn afresh at the start of every run, using that run's seed,
and are already in force on day 1. With \code{rewire_every > 0} they are redrawn
during the run, taking effect the following day. Because the intervention
keeps a single shared state, replicates with \link{run_multiple} must use one
thread (\code{nthreads = 1}); \code{bubbles()} flags the model accordingly.
}

\subsection{What this cannot represent}{

Both rules produce \emph{exclusive} bubbles, as the modelled policies prescribe. A
rule that instead gives each person a personal budget of contacts that need be
neither mutual nor exclusive -- "up to ten different people a week", say -- is
not a partition of the population and cannot be expressed this way.

Fixed out-of-bubble "sport partners" and travel between regions are not yet
modelled either.
}

The function \code{sample_household_sizes()} draws a vector of household sizes
that sum to \code{n_agents}, with each size between \code{min_size} and \code{max_size}. It
ensures that the total number of agents is exactly \code{n_agents} by adjusting
the last household size if necessary.
}
\examples{
# A network SEIR model on a small-world contact network
model <- ModelSEIR(
  name              = "Flu",
  prevalence        = 0.01,
  transmission_rate = 0.1,
  incubation_days   = 4.5,
  recovery_rate     = 1 / 7
)

agents_smallworld(model, n = 2000, k = 8, d = FALSE, p = 0.05)

# Households of three agents (in agent order)
household_id <- rep(seq_len(ceiling(2000 / 3)), each = 3)[1:2000]

# Two households per bubble from day 10, halving within-bubble transmission
bubbles(
  model,
  household_id        = household_id,
  flavor              = "household",
  group_size          = 2,
  transmission_factor = 0.5,
  start_day           = 10
)

run(model, ndays = 100, seed = 55)
model

# Other policies:
#
# Strict household-only lockdown (a common reference scenario):
#   bubbles(model, household_id, "household", group_size = 1)
#
# One close contact per person, joining the two households:
#   bubbles(model, household_id, "peer", group_size = 1)
#
# As above, but the bubbles are drawn again every week:
#   bubbles(model, household_id, "peer", group_size = 1, rewire_every = 7)
#
# Individually chosen bubbles of up to four households:
#   bubbles(model, household_id, "peer", group_size = 2, max_households = 4)
}
\seealso{
\link{global-events}, \link{ModelSEIR}, \code{\link[=agents_smallworld]{agents_smallworld()}}
}
\concept{global-events}
